---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for page in site.pages -%}
  {%- unless page.sitemap.exclude == "yes" or page.sitemap.exclude == true -%}
    {%- assign is_excluded = false -%}
    {%- if page.url contains '.xml' -%}{%- assign is_excluded = true -%}{%- endif -%}
    {%- if page.url contains 'assets' -%}{%- assign is_excluded = true -%}{%- endif -%}
    {%- if page.url contains 'google' -%}{%- assign is_excluded = true -%}{%- endif -%}
    {%- if page.url contains '_posts' -%}{%- assign is_excluded = true -%}{%- endif -%}
    {%- unless is_excluded %}
  <url>
    <loc>{{ site.url }}{{ page.url | remove: 'index.html' }}</loc>
    {%- if page.date %}
    <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
    {%- endif %}
  </url>
    {%- endunless -%}
  {%- endunless -%}
{%- endfor -%}
{%- for post in site.posts %}
  <url>
    <loc>{{ site.url }}{{ post.url }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
  </url>
{%- endfor %}
</urlset>
